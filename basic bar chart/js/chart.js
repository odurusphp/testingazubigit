const monthlySales = () => [
    { month: "January", amount: Math.random() * 99999 },
    // add other objects for each month
];
    
function buildBarChart() {
    const DIV_ITEMS = document.querySelector(".items");
    // Calculate sum of monthly sales using built-in function - reduce() 
    // such objects.reduce((a, b) => { return a + b.property }, 0);
    // get new lists with computed amount for each month using maps

    // Create new div element for each item - January, February, March, etc.
    // Create new paragraph element to be appended by the created div element 
    // Use calculated amount to set the div element's height. 
    // NB: use percentage instead of pixels for height value
    // Good Luck!!!
}

buildBarChart();